name: Docker Image CI

on:
  push:
    branches: [ "action-test" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build -f ./Crawer_104/Dockerfile -t arisuokay/crawer-104:$(date +"%Y.%m.%d.%H.%M.%S") .

